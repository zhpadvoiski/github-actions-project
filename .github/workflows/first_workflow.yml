name: My First Workflow

on: push

jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo "This is my first job"
      - name: echo node version
        run: |
          node -v
          nvm -v
          nvm use
